import React from 'react';
import { Container, LeftSection, RightSection, Text } from './Welcome.styles';

export const Welcome = ({ }) => {
  return (
    <Container>
        <Text large>Vamos a practicar!</Text>
        <Text>Select the language and the word category</Text>
    </Container>
    );
};